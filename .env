# --- Docker Port Mappings ---
# Controls the ports on your host machine (your computer).
FRONTEND_HOST_PORT=3000
BACKEND_HOST_PORT=8000

# --- Application Configuration ---
# This variable tells the Next.js frontend where to find the backend API.
# The prefix NEXT_PUBLIC_ is REQUIRED for Next.js to expose it to the browser.
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000

# The URL for the frontend. The backend will use this for CORS.
FRONTEND_URL=http://localhost:3000
